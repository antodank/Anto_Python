version: "3.9"

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: vectorizer-qdrant
    restart: unless-stopped
    ports:
      - "6332:6333"
    volumes:
      - qdrant_data:/qdrant/storage


volumes:
  qdrant_data:
